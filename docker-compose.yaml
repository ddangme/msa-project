services:
  eureka-server:
    build: ./eureka
    ports:
      - "8761:8761"
    environment:
      - EUREKA_HOSTNAME=eureka-server
  gateway-server:
    build: ./gateway
    ports:
      - "8090:8090"
    depends_on:
      - eureka-server